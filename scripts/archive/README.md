# Archived Scripts

These scripts are one-off historical migrations used during codebase reorganisations.

They are retained for lineage reference and are not executed as part of standard development or `node test.js`.
