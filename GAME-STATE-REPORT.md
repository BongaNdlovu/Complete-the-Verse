# Game State Report — Complete the Verse

**Date:** 2026-07-29  
**Source review:** Game state evaluation (content, practice loop, tutorial, abandon, architecture, daily share)  
**Play entry:** `index.html` (split project; old monolith HTML deleted)  
**Verification:** `node game-structure.test.js` → see session output; browser preview of `index.html`

---

## Summary

Remaining game-state recommendations were executed after the UI pass: monolith split + AI CSS purge, verse bank expansion to the 400–600 band (with full KJV blank/distractor QA regen), Practice Misses mode, first-run tutorial, abandon scoring, and daily share (client-side). Remote leaderboard was not built (needs a backend).

---

## Changes shipped

### 1. Architecture split + AI CSS purge
| Path | Role |
|---|---|
| `index.html` | Markup shell |
| `css/game.css` | Styles (AI visual-system block removed; `biblical-thriller` kept) |
| `js/verses.js` | Base verse bank + `BOOKS_ORDER` |
| `js/verses-extra.js` | Expanded verses + `BY_TIER` rebuild |
| `js/passages.js` | Multi-blank / reconstruct passages |
| `js/game.js` | Game systems |
| `scripts/split-monolith.js` | One-shot splitter (source monolith removed after split) |

Old monolith `complete-the-verse(1) (1).html` **deleted** — play only via `index.html`.

### 2. Verse bank → ~500 KJV entries
- Base: 196 in `js/verses.js`
- Extra: 309 in `js/verses-extra.js` (regenerated by `scripts/build-verse-extra.js` with mid-verse blanks, no empty `p`, answer ≤6 words, no punct-only/word-swap distractors)
- **Total: 505** (target band 400–600)
- Per-book floor: **≥3** verses across all 66 books (asserted in `game-structure.test.js`)
- QA: `node scripts/qa-verses-extra.js` → 0 mechanical flags
- Menu copy uses `VERSES.length` dynamically

### 3. Practice Misses mode
- New mode on the menu: **Practice Misses** (15 verses, 12s × difficulty clock)
- `drawPracticeVerse()` weights never-seen / prior misses / shaky mastery first
- HUD + act-track + pause label for drill progress
- Results kick: “The drill is finished”
- Save: `best.practice`

### 4. First-run tutorial
- Overlay `#tutorial` after boot → menu when `SAVE.set.tutorialDone` is false
- Three steps: read blank, select→Lock, Selah/Illuminate
- CTAs: **Try Practice Misses** / **Enter the hall**
- Persists `tutorialDone: true`

### 5. Abandon = recorded run
- `abandonRun()` calls `endRun("abandon")` if any answers were attempted
- Soft-quit to menu only when `attempts === 0`
- Abandon uses death sting; score kept at 85% of computed total; ending title “Abandoned”

### 6. Daily share (local)
- Results button **Share Daily** (daily mode only)
- Uses `navigator.share` when available, else clipboard copy
- **Not done:** remote/global leaderboard (requires a hosted API)

---

## Verification

```
node game-structure.test.js
```

Expected: `PASS — game structure · verses=… · books=66`

Manual: open `index.html` in the browser — menu shows Practice Misses + verse count; first launch shows tutorial; abandon mid-run reaches results; daily results show Share.

---

## Intentionally left alone / flags

- **Remote daily leaderboard** — no backend configured; share is client-only.
- Auto-generated extras still benefit from occasional human playtesting; mechanical QA gates are clean (`qa-verses-extra.js`).
- Orbitron/Rajdhani font links removed from `index.html` with the AI skin; thriller fonts remain (Cinzel, EB Garamond, Barlow Condensed).
